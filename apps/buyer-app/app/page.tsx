import { AxiosAPI } from "api";

export default function Home() {
  // AxiosAPI.getInstance().billboardService.createBillboard()
  return <div className={"bg-red-50 text-red-800 text-2xl"}>buyer app</div>;
}
